# LLM provider — real OpenAI provider (Step 29)
# Placeholders only; never commit real secrets.
OPENAI_API_KEY=
# Required model identifier, e.g. gpt-4o-mini
OPENAI_MODEL=
# Optional tuning (bounds must be respected at runtime)
# OPENAI_TIMEOUT_SECONDS=60
# OPENAI_MAX_RETRIES=3

# Optional: alternative providers
# ANTHROPIC_API_KEY=

# Deterministic tool paths (optional — tools expected on PATH by default)
# SEMGREP_PATH=
# BANDIT_PATH=
# TRIVY_PATH=
